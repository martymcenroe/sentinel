{
  "name": "pr-sentinel",
  "version": "1.0.0",
  "description": "PR Issue-Reference Enforcer — GitHub App on Cloudflare Workers",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wrangler": "^4.115.0"
  }
}
